module "openstack_keymanager_container_v1" {
  source = "./modules/openstack/d/openstack_keymanager_container_v1"

  # name - (optional) is a type of string
  name = null
  # region - (optional) is a type of string
  region = null
}
